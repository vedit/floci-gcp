quarkus:
  grpc:
    server:
      max-inbound-message-size: 4194304
  log:
    level: WARN
    category:
      "io.floci.gcp":
        level: INFO

floci-gcp:
  port: 4588
  default-project-id: test-project
  storage:
    mode: memory
  services:
    compute:
      enabled: true
      operation-delay-ms: 50
      regions: us-central1,europe-west1
    iamcredentials:
      enabled: true
    sts:
      enabled: true
    datastore:
      enabled: true
    kafka:
      enabled: true
      mock: true
    cloudsql:
      enabled: true
      mock: true
    cloudtasks:
      enabled: true
    cloudrun:
      enabled: true
      mock: true
      execution:
        default-port: 8080
        startup-timeout: 240s
        request-timeout: 300s
        operation-timeout: 300s
        cleanup-timeout: 15s
        url-host-suffix:
    cloudfunctions:
      enabled: true
    monitoring:
      enabled: true
    scheduler:
      enabled: true
      invocation-enabled: true
      tick-interval-seconds: 1
    eventarc:
      enabled: true
    bigquery:
      enabled: true
    gke:
      enabled: true
      mock: true
    serviceusage:
      enabled: true
    resourcemanager:
      enabled: true
    firebaseauth:
      enabled: true
  docker:
    api-timeout: 30s
    resource-namespace: ""
